# gallery.json is runtime-generated by the art-pipeline
# and must never be committed or deployed from this repo
gallery.json

# wrangler local cache + tmp dirs (created during `wrangler deploy`)
.wrangler/
